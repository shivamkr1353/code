<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Upload Successful</title>

    <style>
      /* Google Font */
      @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap");

      /* Global Reset */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
      }

      body {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background: #f5f7fa; /* clean dashboard background */
        padding: 20px;
      }

      /* Main Card */
      .upload-container {
        width: 100%;
        max-width: 430px;
        background: #fff;
        padding: 35px 40px;
        border-radius: 12px;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
        text-align: center;
      }

      /* Success Icon */
      .success-icon {
        font-size: 48px;
        color: #2ecc71;
        margin-bottom: 10px;
      }

      .upload-container h2 {
        font-size: 1.8rem;
        font-weight: 600;
        margin-bottom: 15px;
        color: #222;
      }

      .upload-container p {
        font-size: 1rem;
        color: #555;
        margin-bottom: 25px;
        line-height: 1.5;
      }

      /* View Resume Button */
      .styled-button {
        display: block;
        width: 100%;
        height: 48px;
        line-height: 48px;
        background: #0d6efd;
        color: #fff;
        text-decoration: none;
        border-radius: 6px;
        font-size: 1rem;
        font-weight: 500;
        transition: background 0.3s ease, box-shadow 0.3s ease;
      }

      .styled-button:hover {
        background: #0b5ed7;
        box-shadow: 0 4px 12px rgba(13, 110, 253, 0.25);
      }

      /* Back Link */
      .back-link {
        display: inline-block;
        margin-top: 22px;
        color: #0d6efd;
        font-weight: 500;
        text-decoration: none;
        transition: color 0.3s ease;
      }

      .back-link:hover {
        color: #084298;
        text-decoration: underline;
      }
    </style>
  </head>

  <body>
    <div class="upload-container">
      <div class="success-icon">✔</div>

      <h2>Upload Successful</h2>

      <p>
        Your resume has been uploaded successfully.<br />Click below to view
        your file.
      </p>

      <a href="<%= url %>" class="styled-button" target="_blank">
        View My Resume
      </a>

      <a href="/student/studenthome" class="back-link">← Back to Home</a>
    </div>
  </body>
</html>
